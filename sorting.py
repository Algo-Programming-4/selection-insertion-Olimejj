#bubble(list) - > sorted ist
def bubble(l):
  return sorted(l)

#selection(list) -> sorted list
def selection(l):
  return sorted(l)

#insertion(list) > sorted list
def insertion(l):
  return sorted(l)
